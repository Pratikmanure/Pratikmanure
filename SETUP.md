# 🚀 Setup — Pratik Manure GitHub Profile v2

## 1. Create the profile repository

Your repository must be named exactly:

`pratikmanure`

and it must be public.

## 2. Upload BOTH files

Keep this exact structure:

```text
pratikmanure/
├── README.md
└── assets/
    └── space-banner.svg
```

**Important:** The space image is now a local SVG. Do NOT link it from an AI/image-host URL. Upload `assets/space-banner.svg` to GitHub along with `README.md`.

## 3. Pin your repositories

Recommended order:

1. ARGUS Defense Core
2. Deloitte
3. FraudStream Command Center
4. Algorithmic Trading Systems (if you want the private project represented, use a public case-study repository)
5. strongest ML deployment project
6. DSA / SQL

## 4. Replace project links

The README currently uses your profile link for the ARGUS call-to-action because the exact repository URL was not supplied in the current chat.

After upload, replace that button with the exact repository URL.

## 5. External badge images

The README uses Shields.io and Simple Icons for technology badges. If GitHub blocks external images in a particular environment, the content still remains readable because the technology names are also present as text.

## 6. What makes this version different

- Local self-contained space SVG
- Expandable `<details>` sections
- Interactive-looking technology stack
- Clickable badges
- Structured project cards
- Clear current vs future skills
- Cloud Engineer included as a role
- Public/private project status is explicit
- No fake certificate claims
- Strong academic section without dumping the entire transcript
