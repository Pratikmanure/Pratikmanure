# GitHub Profile README — Setup

1. Create/open the public repository named exactly `pratikmanure`.
2. Put `README.md` in the repository root.
3. Put `assets/space-banner.png` in `assets/`.
4. Commit and push.
5. In the GitHub profile, pin the strongest repositories:
   - ARGUS Defense Core
   - Deloitte case study (private/public case-study repo as appropriate)
   - FraudStream Command Center
   - Algorithmic Trading Systems
   - strongest ML/AI deployment project
   - DSA/SQL repository
6. Replace any project URLs/placeholders with the real repository links after the repositories are ready.
7. Only add certifications after they are actually completed and verifiable.
